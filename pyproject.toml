[project]
name = "travelbook"
version = "0.1.0"
description = "Generate a beautiful travel PDF from a JSON itinerary."
readme = "README.md"
requires-python = ">=3.10"
license = { text = "MIT" }
authors = [{ name = "Hugo Rybinski", email = "hugo.rybinski@algolia.com" }]
dependencies = [
    "fpdf2>=2.7",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
]

[project.scripts]
travelbook = "travelbook.cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/travelbook"]
artifacts = ["src/travelbook/fonts/*.ttf"]

[tool.pytest.ini_options]
testpaths = ["tests"]
