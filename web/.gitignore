node_modules
dist
dev-dist
*.local
*.tsbuildinfo
.DS_Store

# Built Python wheel + its manifest (regenerate with `npm run wheel`)
public/py/*.whl
public/py/wheel.json

# Build-version stamp written by the pre-push hook (see .githooks/pre-push)
.commit-info.json
